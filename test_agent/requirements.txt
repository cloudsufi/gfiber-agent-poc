# Runtime dependencies for the demo agent.
#
# Install the adk-tools package from the repo (editable, recommended for dev):
#   pip install -e "..[all]"
#
# Or install from GitHub once published:
#   pip install "git+https://github.com/cloudsufi/adk-tools@main[all]"

# Core package — install from parent directory during development
../

# ADK web server + Gemini support
google-adk>=0.4

# Optional extras — uncomment as needed:
# google-cloud-secret-manager>=2.0    # auth from GCP Secret Manager
# google-auth>=2.30                   # service_account auth
# httpx>=0.27                         # OAuth2 client-credentials auth
# jsonschema>=4.17                    # input/output schema validation
