type: object
properties:
  tool_name: { type: string }
  endpoint:  { type: string }
  mock:      { type: boolean }
  echo:
    type: object
    properties:
      query:      { type: string }
      top_k:      { type: integer }
      filter_tag: { type: string }
